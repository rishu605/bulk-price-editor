node_modules

# macOS
.DS_Store

/.cache
/build
/app/build
/public/build/
/public/_dev
/app/public/build
/prisma/dev.sqlite
/prisma/dev.sqlite-journal
database.sqlite

.env
.env.*
!.env.example

# package-lock.json is committed deliberately: CI needs `npm ci` for reproducible builds.
yarn.lock
pnpm-lock.yaml

/extensions/*/dist

# Ignore shopify files created during app dev
.shopify/*
.shopify.lock

# Hide files auto-generated by react router
.react-router/

chaos/artefacts/

# The introspected Admin schema, 6MB of generated JSON. Regenerated by
# `npm run graphql-codegen`; the operation types beside it are committed because they
# are what typechecking reads.
app/types/admin-*.schema.json
perf-baseline-*.json
